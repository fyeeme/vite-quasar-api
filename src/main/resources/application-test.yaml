server:
  port: 9020
spring:
  datasource:
    password: root
    url: jdbc:mysql://127.0.0.1:3306/quasar
    username: root

logging:
  level:
    web: TRACE
management:
  endpoints:
    web:
      base-path: /manage
      exposure:
        include: '*'
